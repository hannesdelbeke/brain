classes register themself with a central manager

using this approach
there might be no need for a collector that searches subclasses.
it still might be needed if you want to load all .py files in a folder
