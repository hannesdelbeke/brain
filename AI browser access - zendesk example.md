### Use case example
if AI can access browser, it can go to zendesk, and check my tickets.

- there is a [[MCP server|MCP]] for zendesk, but it's paid? 
- you need IT admin permission, local browser is a [[workaround]]

With [[MCP for browser]] enabling browser access for AI,
i could turn this into a [[agent skill]]. above text tells claude how to launch [[Chrome|chrome]]. 
then add zendesk url and instructions e.g. where to find tickets.
